{
  "name": "wpp-sistem-multitenant",
  "version": "3.0.0",
  "description": "Sistema multi-tenant de aquecimento e disparo WhatsApp com painel admin",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "postinstall": "npm --prefix backend install --omit=dev && npm --prefix frontend install && npm --prefix frontend run build",
    "backend:install": "npm --prefix backend install",
    "frontend:install": "npm --prefix frontend install",
    "frontend:build": "npm --prefix frontend run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "keywords": [
    "whatsapp",
    "warming",
    "dispatch",
    "multi-tenant",
    "saas"
  ],
  "license": "MIT"
}
